Regenerate the repository from the cookieplone `monorepo_addon` template, adding `repository.toml`, `docker-compose.yml`, a top level `Makefile` and dedicated GitHub Actions workflows for backend, frontend and documentation. @ericof
